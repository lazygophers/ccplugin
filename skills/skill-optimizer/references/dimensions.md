# 9 维 Rubric 全表 + HL 杠杆

> 适配自 darwin-skill v2.0（`~/.claude/skills/darwin-skill/SKILL.md`），其设计依据 SkillLens（arXiv 2605.23899）。总分 100。

## 结构维度（59 分）— 静态分析

| # | 维度 | 权重 | 评分标准 | 红灯信号 |
|---|------|------|---------|---------|
| 1 | **Frontmatter 质量** | 7 | name 规范 / description 做什么+何时用+触发词 / **项目底线 < 512 字符** / when_to_use < 128 | 结尾「灵活应用/根据情况判断」空话尾巴 |
| 2 | **工作流清晰度** | 12 | 步骤有序号、每步输入/输出明确、可执行 | 步骤无序、缺输入输出契约 |
| 3 | **失败模式编码** | 12 | **显式 if-then 失败分支**（三段式：触发/一线修复/兜底） | 只写正向流程不写失败分支（扣 ≥3） |
| 4 | **检查点设计** | 6 | 关键决策前用户确认，**🔴/🛑/STOP 显性视觉标记** | 仅靠「如果...建议...」措辞（不算） |
| 5 | **可执行具体性** | 17 | 具体参数/格式/示例，可直接执行 | 「建议/可以考虑/根据情况/灵活把握」≥3 处（扣 ≥3） |
| 6 | **资源整合度** | 4 | references/scripts 路径可达、引用正确 | 孤立文件未在 SKILL.md 引用 |

## 效果维度（35 分）— 需实测

| # | 维度 | 权重 | 评分标准 | 红灯信号 |
|---|------|------|---------|---------|
| 7 | **整体架构** | 12 | 层次清晰、不冗余不遗漏、runtime 中立 | 「说白了/换句话说/综上」AI 腔（一处扣 1） |
| 8 | **实测表现** | 23 | test prompt 跑一遍，输出质量符合宣称 | 无 test prompt 凭空打分 |

## Meta-skill 维度（6 分）— 反例与黑名单

| # | 维度 | 权重 | 评分标准 | 红灯信号 |
|---|------|------|---------|---------|
| 9 | **反例与黑名单** | 6 | 「不要做什么」反例清单 + 危险动作单独章节 | 只写「应该做 X」无「不要做 Y」（扣 ≥3） |

## 评分规则

- dim 1-7、9：每维打 1-10 分，乘权重得该维得分
- dim 8：跑 2-3 test prompt，按输出质量打 1-10
- **总分 = Σ(维度分 × 权重) / 10**，满分 100
- 改进后总分**严格高于**改进前才保留（ratchet）

## 实证基础（SkillLens 转述）

darwin-skill 自引 SkillLens 数据（非本 skill 独立复现）：
- LLM-as-judge 准确率仅 **46.4%**（接近随机）
- 加入 meta-skill 三维度（dim3 失败模式 / dim5 具体性 / dim9 反例黑名单）后升到 **73.8%**
- darwin 本机 controlled study：huashu-research 4 类 degradation × 5 独立 judge 盲测，一致 V1>V2，Δ 均值 +46.5（5/5 high confidence）

**结论**：rubric 识别 gross degradation 可靠，fine-grained 差异不可信，**重要决策必须人审**。

## HL 杠杆速查（high-leverage，4 行撬动 +3 分）

| HL | 维度 | 杠杆 | 落地 |
|----|------|------|------|
| **HL-1** | dim4 | 显性视觉标记 | 加 🔴 CHECKPOINT / 🛑 STOP，靠「必须」措辞无效——LLM 扫标记优先于语义 |
| **HL-2** | dim3 | if-then 三段式 fallback 表 | 「症状/解法」两列 → 「触发条件/一线修复/兜底」三段式 |
| **HL-3** | Phase 1 诊断 | 维度相关簇警告 | dim2/dim3/dim4 是相关簇——修 dim3 时 dim2 常跟涨；找最低维度时同时看相关簇短板 |
| **HL-4** | Phase 4 退出 | 触顶自动 break | 连续 2 轮 Δ < 2 → break；+0.15 是停手信号非继续信号 |

## 相关簇（HL-3 详解）

| 簇 | 维度 | 联动原因 |
|----|------|---------|
| 工作流簇 | dim2 / dim3 / dim4 | 工作流清晰度 ↔ 失败分支 ↔ 检查点，改其一常推另两个 |
| 表达簇 | dim5 / dim7 | 具体性 ↔ 架构冗余，删 AI 腔同时提升两维 |
| 触发簇 | dim1 / dim9 | description 收窄 ↔ 反例黑名单，边界清晰同时影响 |

**操作**：诊断时找最低维度 → 检查其所在簇的其他维度是否同步短板 → 决定单点修还是簇级修。
