---
title: writing-style
layer: recall
category: impl
keywords: [positive,表述,文档编写,AI,硬护栏,对照式,范式,硬约束,单变量,轮,优化,归因]
status: active
---

## 正向表述优先原则

### 铁律 / 契约

- MUST：AI 文档默认正向表述"该做什么"，禁负向"别做什么"列表
- MUST：仅无法正向化的硬护栏保留反例，且必须配正例对照
- MUST：反例段命名含"被拒模式 + 原因 + 正例"三要素
- MUST：禁裸"不要做 Y"黑名单，每条禁令必配正例

### 反例 (命中=违规)

| 禁 | 改为 |
|---|---|
| "组件目录禁进 `.claude-plugin/`" | "组件在插件根；`.claude-plugin/` 仅放 `plugin.json`" |
| "禁 hook `exit 1`" | "guard 用 `exit 2` 阻断；副作用 hook 失败 `exit 0` 兜底" |
| 列表式"禁 A、禁 B、禁 C" | 表格式"禁 A | 正例 A；禁 B | 正例 B；禁 C | 正例 C" |
| 独立"不要做什么"章节 | 融入正向流程，仅必要硬护栏处配对照表 |

## 硬护栏范式

### 硬护栏范式

### 触发场景

编写 skill/agent/command 时需要硬约束（无法正向化的禁令）—— 如安全边界、防错纪律、接线规则等。

### 陷阱 / 正解

❌ "硬规" + 列表式 "禁 A、禁 B、禁 C"  
根因：负向表述难理解，AI 易忽略禁令细节或不知正确做法  
✅ "硬护栏" + 表格式 "禁 A | 正例 A；禁 B | 正例 B；禁 C | 正例 C"

### 反例

❌ "组件目录禁进 `.claude-plugin/`" — 单向禁令，AI 不知该放哪  
✅ "组件塞进 `.claude-plugin/` 目录 | 组件在插件根；`.claude-plugin/` 仅放 `plugin.json`" — 双向对照，理解禁令同时知道正确做法

### 案例

- plugin-dev SKILL.md refactor (cf200526): 从"🔴 硬规（违反即失效）"改"硬护栏（无法正向表述的禁令，配正例）"，对照表提升可执行性
- skill-dev SKILL.md: 从列表式"禁..."改表格式"禁 | 正例"

### 适用

- 无法正向化的硬约束（安全/防错/边界）
- 需要"禁 X 且配正例"的场景
- 任何 skill/agent/command 的硬规部分

### 关联

[[writing-style#正向表述优先原则]] (core, 正向表述优先原则)

## 单变量轮模式

### 单变量轮模式

### 触发场景

优化循环（评分 → 改进 → 验证）—— 防止多维同改导致归因失效，无法确定哪项改动起效。

### 陷阱 / 正解

❌ 一轮改多个维度（如同时改触发词、结构、失败模式）  
根因：改后如果总分上升，无法确定是哪一项改动起效；如果下降，也不知道哪项需要回滚  
✅ 一轮只改一维度（或一紧密相关簇）

### 反例

❌ "本轮改触发词 + 结构 + 失败模式，希望总分 +5" → 改后总分 +3，不知道哪项起效哪项拖后腿  
✅ "本轮只改触发词，目标 Δ>0；下一轮改结构" → 每轮结果清晰可归因

### 案例

- optimize-any skill (已删除): 硬规 3 "单变量轮：一轮只改一维度（或一紧密相关簇），多维同改归因失效"
- plugin-dev optimize-rubric: "单变量轮: 一轮改一维度 (或一相关簇如 2/3/4 接线)"

### 适用

- 任何优化循环（评分 → 改进 → 验证 → ratchet）
- 实验性改动（需确定因果关系）
- A/B 测试场景

### 关联

[[verification#独立验证防自评偏差]] (core, 独立验证防自评偏差)
