# agent 文档正向化 — 原文→新文 对照表 (skein-checker / skein-specer / skein-dedup)

范式见 design.md §1 四类转法：唯一合法路径(禁A→只B) / 前置条件(禁未X就Y→先X再Y) / 归属边界(禁做A→A归谁) / 能力事实(保留原样)。

## skein-checker.md

| 原文 | 新文 | 类型 | why 保留 |
|---|---|---|---|
| task 级验收项核实通过 ... → 立即回写勾选, 禁攒到最后 | 验证通过即时回写勾选, 逐条办完——攒到最后一并处理会漏检 | 唯一合法路径 | 攒到最后会漏检（新增显式化原隐含理由） |
| 手工验证 → 标 MANUAL 需人审, 禁臆判 pass | 只标 MANUAL 交人审——非机验场景没有可机判的 pass 依据 | 唯一合法路径 | 非机验场景无可机判依据 |
| 任一条 FAIL → 上报, 禁放过 | 任一条 FAIL → 必须上报, 逐条不漏 | 唯一合法路径 | （原无显式 why，语义保持“不漏”） |
| 任一 fail → 上报 (main 派修复), 禁放过 | 任一 fail → 必须上报 (main 派修复), 逐条不漏 | 唯一合法路径 | 同上 |
| 全启发式候选, 禁断言违规 | 只作候选提出, 非断言 | 唯一合法路径 | 启发式候选不能升级为断言 |
| exec/check/finish 阶段禁直接改 design.md；方案性问题按 flow-loop 回 planning/用户裁定 | design.md 的方案性改动归 planning/用户裁定处理, 按 flow-loop 回流 | 归属边界 | 方案权归 planning/用户 |
| 未全绿不得 finish | 全绿才可 finish | 前置条件 | 先全绿再 finish |
| 不在 check 宣告 Done | Done 宣告归 finish 阶段, check 阶段只出验证结果 | 归属边界 | Done 判定权归 finish 阶段 |
| 禁把错误输出当结果返回 | 只把 `[工具失败: <原因>]` 当结果回传——原始错误输出不是有效结果 | 唯一合法路径 | main 消费错误摘要当数据会静默降级 |
| 无 Write/Edit | 无 Write/Edit (能力边界) | 能力事实(保留) | 陈述能力事实, 无正向等价 |
| 仅限勾选验收项, 不改内容 | 仅限勾选验收项, 内容保持原样 | 唯一合法路径 | 写盘范围限定 |
| 禁就地改、禁自行加 subtask、禁绕过 main 重派 executor | 就地改归后续 executor、补 subtask 归 main、重派 executor 归 main | 归属边界 x3 | 各动作的权责主体 |
| 禁臆判 pass, 标 MANUAL 需人审 | 只标 MANUAL 交人审, 机判 pass 无依据 | 唯一合法路径 | 机判 pass 无依据 |
| 禁 create/start/finish/archive 等越权命令 | create/start/finish/archive 等生命周期命令归 main | 归属边界 | 生命周期管理权归 main |
| 禁臆判 pass (失败模式表) | 只判 MANUAL, 机判 pass 无依据 | 唯一合法路径 | 同上 |
| 全部逐条报, 禁漏 (失败模式表) | 全部逐条上报, 一条不漏 | 唯一合法路径 | 保持“不漏”语义 |

## skein-specer.md

| 原文 | 新文 | 类型 | why 保留 |
|---|---|---|---|
| 写盘全经 skein-spec CLI, 禁手改文件 | 写盘全经 `skein-spec` CLI 完成 (手改文件不在允许路径内) | 唯一合法路径 | 唯一写盘路径 |
| 本 agent 不做召回 (归 skein-recaller) | 召回归 skein-recaller | 归属边界 | 职责边界 |
| 必须并入同一文件 (禁一规则一文件) | 必须并入同一文件 (一规则一文件的粒度不成立) | 唯一合法路径 | 粒度定义本身排斥该拆法 |
| 改走 sediment 建新章节, 禁静默追加 | 改走 sediment 显式建新章节——追加只能经此路径完成, 不静默并入旧章节 | 唯一合法路径 | 新增必须显式可追溯 |
| 断链只报告不修 ... 禁自动改任一头 | 断链只报告不修 ... 改哪头归人工判断决定 | 归属边界 | 无从自动决断, 需人判 |
| 禁把错误输出当结果返回 | 只把 `[工具失败: <原因>]` 当结果回传——原始错误输出不是有效结果 | 唯一合法路径 | main 消费错误摘要当数据会静默降级 |
| 禁自动改任一头 (失败模式表) | 改哪头归人判 | 归属边界 | 同上 |

## skein-dedup.md

| 原文 | 新文 | 类型 | why 保留 |
|---|---|---|---|
| 写盘全经 skein CLI 自动处置, 禁手改 task.json | 写盘全经 `skein` CLI 自动处置 (task.json 不经手改路径) | 唯一合法路径 | 唯一写盘路径 |
| 先读全量再迁, 禁凭记忆 | 先读全量再迁, 迁移只依据当次 Read 结果 | 前置条件 | 记忆非可信依据 |
| 判据不足的 task 不归并; 判不准是否相关 → 不连 (宁缺毋滥) | 判据不足的 task 保持独立; 判不准是否相关时保持不连 (宁缺毋滥) | 唯一合法路径 | 宁缺毋滥 |
| 报错即该连法非法, 换或跳过, 禁强连 | 报错即该连法非法, 只能换方向或跳过 | 唯一合法路径 | 非法连法无强连余地 |
| CLI 报错禁当成功继续 | CLI 报错时只标 `[工具失败: <原因>]` 并停止该路径——原始错误不是成功结果 | 唯一合法路径 | main 消费错误摘要当数据会静默降级 |

## 豁免清单 (无正向等价 / 非本任务范围的残留匹配)

| 位置 | 原文 | 理由 |
|---|---|---|
| skein-checker.md `tools:` frontmatter (未改) | 无 Write/Edit | 能力事实陈述, 非约束, 无正向等价, PRD 边界已明确豁免 |
| skein-specer.md frontmatter (未改) | 无 Write/Edit | 同上 |
| skein-dedup.md L80(改写后) `判不准是否相关` | 「判不准」= 判断不确定/不精准, 非「禁止」义, 命中 grep `不准` 属误报, 不转写 |
| skein-dedup.md L89 `skipped` JSON 示例字段 `<判不准/CLI 拒 的连法 + 原因>` | 同上, JSON 示例字段值, 非约束句, 误报 |

## 未改动确认

三文件章节结构 (`##`/`###` 标题) 与 frontmatter 逐字未动, 仅正文措辞替换 (`git diff --stat`: checker +12/-12, specer +6/-6, dedup +5/-5, 无 `#` 行改动)。
