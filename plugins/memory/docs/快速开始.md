# Memory Plugin 快速开始

> 版本：0.1.0
> 前提条件：Python 3.10+, uv, Claude Code 1.0.0+

## 安装

### 1. 进入插件目录

```bash
cd plugins/memory
```

### 2. 创建虚拟环境

```bash
uv venv
source .venv/bin/activate  # Linux/macOS
# .venv\Scripts\activate   # Windows
```

### 3. 安装依赖

```bash
uv pip install -e ".[dev]"
```

### 4. 运行测试（可选）

```bash
uv run pytest -v
```

预期输出：
```
✓ 6/6 tests passed
✓ Coverage: 85%+
```

## 配置 Claude Code

### 方法 1：手动配置

编辑 `~/.claude/settings.json`：

```json
{
  "plugins": [
    {
      "path": "/absolute/path/to/ccplugin/plugins/memory"
    }
  ]
}
```

### 方法 2：使用 Claude Code 命令

```bash
claude plugin install /path/to/ccplugin/plugins/memory
```

## 验证安装

### 1. 重启 Claude Code

```bash
# 退出并重新启动 Claude Code CLI
```

### 2. 测试工具调用（在 Claude Code 中）

```
请使用 memory_store 工具存储一条记忆：这是测试记忆
```

预期响应：
```
已存储记忆: 这是测试记忆 (标签: 无)
```

## 基本使用

### 存储记忆

```python
# 简单存储
memory_store(content="重要信息")

# 带标签和元数据
memory_store(
    content="项目架构决策：使用微服务架构",
    tags=["架构", "决策"],
    metadata={"project": "ccplugin", "date": "2025-01-15"}
)
```

### 搜索记忆

```python
# 简单搜索
memory_search(query="架构")

# 带标签过滤
memory_search(
    query="微服务",
    tags=["架构"],
    limit=5
)
```

## 开发模式

### 运行 MCP Server（调试）

```bash
# 方式 1：使用 uv
uv run python -m memory.server

# 方式 2：直接运行
source .venv/bin/activate
python -m memory.server
```

### 查看日志

```bash
# 设置调试日志
export LOG_LEVEL=DEBUG

# 运行服务器
uv run python -m memory.server
```

## 故障排查

### 问题：MCP Server 未启动

**检查步骤**：

1. 确认 Python 版本：
```bash
python --version  # 应该 >= 3.10
```

2. 确认依赖已安装：
```bash
uv pip list | grep mcp  # 应显示 mcp>=1.1.0
```

3. 查看 Claude Code 日志：
```bash
~/.claude/logs/
```

### 问题：工具未显示

**可能原因**：

1. 配置路径错误：
```bash
cat ~/.claude/settings.json  # 检查 path 是否正确
```

2. 插件未正确安装：
```bash
ls -la /path/to/ccplugin/plugins/memory/.claude-plugin/plugin.json
```

### 问题：导入错误

**解决方法**：

确保在虚拟环境中：
```bash
which python  # 应指向 .venv/bin/python
source .venv/bin/activate
```

## 下一步

- 查看 [工具参考](./工具参考.md) 了解详细参数
- 查看 [开发指南](./开发指南.md) 学习插件开发
- 查看 [架构设计](./架构设计.md) 理解实现原理

## 支持

遇到问题？

1. 查看 [GitHub Issues](https://github.com/lazygophers/ccplugin/issues)
2. 创建新 Issue 并提供：
   - 系统信息（OS、Python 版本）
   - 错误日志
   - 复现步骤

---

**最后更新**：2025-01-15
**维护者**：luoxin
