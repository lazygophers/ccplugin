---
name: mermaid-mindmap
description: Mermaid 思维导图语法规范，包括节点定义、形状、图标、样式和布局配置
---

# Mermaid 思维导图 (Mindmap)

思维导图用于可视化组织信息，展示层级关系和概念结构。

## 基本语法

```mermaid
mindmap
  root((中心主题))
    分支一
      子节点A
      子节点B
    分支二
      子节点C
```

## 节点定义

### 缩进规则

使用缩进定义层级关系：

```mermaid
mindmap
    根节点
        一级节点
            二级节点
                三级节点
```

### 根节点

```mermaid
mindmap
  root((思维导图))
    分支A
    分支B
```

## 节点形状

### 矩形

```mermaid
mindmap
    id[我是矩形]
```

### 圆角矩形

```mermaid
mindmap
    id(我是圆角矩形)
```

### 圆形

```mermaid
mindmap
    id((我是圆形))
```

### 旗帜

```mermaid
mindmap
    id))我是旗帜((
```

### 云形

```mermaid
mindmap
    id)我是云形(
```

### 六边形

```mermaid
mindmap
    id{{我是六边形}}
```

### 默认形状

```mermaid
mindmap
    我是默认形状
```

## 形状语法总结

| 语法 | 形状 |
| --- | --- |
| `id[文字]` | 矩形 |
| `id(文字)` | 圆角矩形 |
| `id((文字))` | 圆形 |
| `id))文字((` | 旗帜 |
| `id)文字(` | 云形 |
| `id{{文字}}` | 六边形 |
| `文字` | 默认 |

## 图标

### 添加图标

```mermaid
mindmap
  root((主题))
    分支A
    ::icon(fa fa-book)
    分支B
    ::icon(mdi mdi-skull-outline)
```

### 图标语法

```
::icon(图标类名)
```

常用图标库：
- Font Awesome: `fa fa-*`
- Material Design Icons: `mdi mdi-*`

## 样式类

### 定义样式类

```mermaid
mindmap
  root((主题))
    A[A]
    :::urgent
    B(B)
    C

    classDef urgent fill:#f00,color:white
```

### 多类样式

```mermaid
mindmap
  root((主题))
    A[A]
    :::urgent large
    B(B)

    classDef urgent fill:#f00
    classDef large font-size:20px
```

### 样式语法

```
节点:::类名1 类名2
```

## Markdown 格式

### 格式化文本

```mermaid
mindmap
    id1["`**粗体** 和 *斜体*
支持换行
Unicode: 🤓`"]
```

### 格式语法

| 格式 | 语法 |
| --- | --- |
| 粗体 | `**文字**` |
| 斜体 | `*文字*` |
| 换行 | 直接换行 |

## 完整示例

### 示例一：项目规划

```mermaid
mindmap
  root((项目规划))
    需求分析
      用户调研
      竞品分析
      需求文档
    设计阶段
      架构设计
      UI设计
      数据库设计
    开发阶段
      前端开发
      后端开发
      接口联调
    测试阶段
      单元测试
      集成测试
      用户验收
```

### 示例二：知识体系

```mermaid
mindmap
  root((编程语言))
    Python
      数据分析
      机器学习
      Web开发
    JavaScript
      前端开发
      Node.js
      移动开发
    Java
      企业应用
      Android
      大数据
    Go
      云原生
      微服务
      系统编程
```

### 示例三：带图标和样式

```mermaid
mindmap
  root((学习计划))
    编程
    ::icon(fa fa-code)
      Python
      JavaScript
      Go
    设计
    ::icon(fa fa-paint-brush)
      UI设计
      UX设计
    运维
    ::icon(fa fa-server)
      Docker
      Kubernetes
      CI/CD
```

## 布局配置

### Tidy Tree 布局

```mermaid
---
config:
  layout: tidy-tree
---
mindmap
root((主题))
  A
  B
  C
  D
```

## 注意事项

### 缩进一致性

- 使用空格缩进
- 保持缩进一致
- 避免混用 Tab 和空格

### 实验性功能

思维导图是实验性功能：
- 语法可能变化
- 图标集成是实验部分
- 建议关注版本更新

## 最佳实践

### 结构设计

- 根节点主题明确
- 层级不宜过深（建议 ≤ 4 层）
- 同级节点数量适中

### 内容组织

- 节点文字简洁
- 合理使用形状区分类型
- 使用图标增强可读性

### 样式应用

- 统一使用样式类
- 避免过度装饰
- 保持视觉一致性

## 参考链接

- [Mermaid 官方文档 - Mindmap](https://mermaid.js.org/syntax/mindmap.html)
