# Task Plugin - 任务管理插件

开发任务的创建和管理系统。

## 功能

- `task_create` - 创建任务
- `task_list` - 列出任务

## 安装

```bash
cd plugins/task
uv venv && source .venv/bin/activate
uv pip install -e ".[dev]"
```

## 许可证

MIT License

**版本**: 0.1.0
