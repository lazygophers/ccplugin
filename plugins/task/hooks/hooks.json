{
  "description": "任务管理插件 Hooks - 自动初始化任务数据库和环境",
  "hooks": {
    "SessionStart": [
      {
        "type": "command",
        "command": "cd ${CLAUDE_PLUGIN_ROOT} && uv run scripts/task.py init --silent || true"
      }
    ],
    "UserPromptSubmit": [
      {
        "type": "command",
        "command": "cd ${CLAUDE_PLUGIN_ROOT} && uv run scripts/task.py init --silent || true"
      }
    ]
  }
}
