---
description: 列出任务
---

调用 `task_list`，参数：$ARGUMENTS

**过滤**: status(open/in_progress/blocked/deferred/closed), assignee, task_type, priority
**选项**: brief=true(简要格式，节省token), limit=20
**建议**: 大多数场景用 brief=true
