# skein-refactor 反例目录

破坏式重构中禁止的做法与正确改法:

| 禁 | 改为 |
|---|---|
| 破坏契约只改一半站点 | grep 穷举, 全站点一次性改齐 |
| 破坏式还留 deprecated 垫片 | 要么真删, 要么退回修补式 |
| 无授权主动破坏兼容 | 默认修补, 破坏需用户/PRD 显式授权 |
| 改前不查上游爆炸半径 | impact/grep 先查, 高风险报用户 |
