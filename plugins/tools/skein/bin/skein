#!/usr/bin/env bash
# skein — thin wrapper forwarding to scripts/skein.py
root="${CLAUDE_PLUGIN_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
exec python3 "~/.claude/plugins/marketplaces/ccplugin-market/plugins/tools/skein/scripts/skein.py" "$@"
