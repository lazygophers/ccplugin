# Task - 任务管理插件

提供任务管理框架，支持任务规划、执行、验证和迭代。不提供具体实现 agents，使用其他插件或用户自定义的 agents。

## 核心特性

- **任务管理框架**：提供任务管理的规范和流程，不绑定具体 agents
- **循环迭代执行**：6 步循环流程（信息收集→计划设计→计划确认→任务执行→结果验证→失败调整），持续迭代直到目标达成
- **灵活的 Agent 系统**：使用其他插件或用户自定义的 agents 执行具体任务
- **Team Leader 模式**：/loop 命令作为 team leader 统一管理所有调度和提问

## 快速开始

### 安装

```bash
/plugin install task@ccplugin-market
```

### 命令

| 命令 | 说明 |
|------|------|
| `/loop [任务目标]` | 进入循环迭代模式，作为 team leader 执行完整流程 |
| `/add [补充内容]` | 补充任务说明、纠正方向或添加约束 |
| `/cancel` | 取消当前执行，保留已完成工作 |

### 典型工作流

```
用户：/loop 实现用户认证功能

→ 步骤 1：信息收集
  - 读取项目文件、文档、CLAUDE.md
  - 确认技术栈和约束
  - 向用户确认不明确的需求

→ 步骤 2：计划设计
  - 分解为原子子任务
  - 建立依赖关系（DAG）
  - 为每个任务分配 agent 和 skills

→ 步骤 3：计划确认
  - 输出执行流程图（ASCII 图形展示依赖关系）
  - 输出验收标准和简要说明
  - 用户确认计划

→ 步骤 4：任务执行
  - 判断任务数量（1 个任务不创建 team，多个任务创建 team）
  - 按依赖顺序调度执行
  - 支持并行/串行编排（最多 2 个并行）
  - 实时输出进度
  - 执行完成后删除 team（如果创建了）

→ 步骤 5：结果验证
  - 检查所有验收标准
  - 全部通过 → 完成
  - 部分失败 → 步骤 6

→ 步骤 6：失败调整
  - 分析失败原因
  - 决定下一步策略
  - 回到步骤 2 重新规划

→ 完成
  - 输出最终报告
  - （team 已在步骤 4 结束时删除）
```

### 计划输出示例

步骤 3 会输出 ASCII 流程图展示任务依赖关系：

```
## 执行计划

### 执行流程图
┌─────────────────────────────────────┐
│ T1: 实现用户模型                      │
│ agent: coder                        │
│ files: src/models/user.py           │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ T2: 实现 API 接口                    │
│ agent: coder                        │
│ files: src/api/auth.py              │
└──────────────┬──────────────────────┘
               ↓
┌─────────────────────────────────────┐
│ T3: 编写测试                         │
│ agent: tester                       │
│ files: tests/test_auth.py           │
└─────────────────────────────────────┘

### 验收标准
- [ ] 所有测试通过
- [ ] Lint 无错误
- [ ] 功能符合需求

### 简要说明
分 3 个子任务实现用户认证功能，先建模型再实现接口最后补测试。
```

## 设计原则

1. **框架优先** - 提供任务管理框架和规范，不提供具体实现 agents
2. **Agent 灵活** - 使用其他插件或用户自定义的 agents 执行具体任务
3. **Leader 统一** - /loop 命令作为 team leader 统一管理所有调度和提问
4. **消息上报** - Agents 通过 SendMessage 向 leader 上报，leader 通过 AskUserQuestion 向用户提问
5. **原子任务** - 每个任务必须是原子性的、不可再分的
6. **并行控制** - 最多 2 个任务并行，不修改同一文件或模块
7. **按需创建 Team** - 只有 1 个任务时不创建 team，多个任务时在步骤 4 创建，执行完成后立即删除

## 目录结构

```
task/
├── commands/
│   ├── loop.md      # 主流程 (team leader)
│   ├── add.md       # 调整任务
│   └── cancel.md    # 中断执行
├── skills/
│   ├── core/        # 核心概念和规范
│   ├── gather/      # 信息收集规范
│   ├── plan/        # 计划设计规范
│   ├── execute/     # 任务执行规范
│   ├── verify/      # 结果验证规范
│   └── loop/        # 循环控制规范
└── README.md
```

## Skills

| Skill | 说明 |
|-------|------|
| **core** | 任务管理生命周期、状态机、角色分工 |
| **gather** | 信息收集策略、工具使用、确认流程 |
| **plan** | 任务分解、依赖建模、agents 分配 |
| **execute** | 并行编排、团队管理、进度跟踪 |
| **verify** | 验收标准检查、质量验证 |
| **loop** | 6 步循环流程、迭代策略、终止条件 |

## Agent 来源

Task 插件不提供 agents，使用以下来源的 agents：
- **其他插件提供的 agents**：如 golang、python、flutter 插件提供的专业 agents
- **用户或项目自定义的 agents**：在项目中定义的专用 agents
- **通用 agents**：如 general-purpose、Explore

## 终止条件

| 条件 | 触发 | 行为 |
|------|------|------|
| 目标达成 | 步骤 5 全部通过 | 正常退出，输出报告 |
| 停滞过多 | 连续 3 次相同错误 | 请求用户指导后继续（不退出） |
| 用户中断 | 用户主动中断 | 根据用户指令处理 |

**重要**：
- 无最大迭代次数限制
- 停滞时请求用户指导，但不退出循环
- 获得用户指导后重置停滞计数器，继续执行

## 许可证

AGPL-3.0-or-later
