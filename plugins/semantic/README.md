# Semantic - 代码语义搜索插件（混合架构版）

基于向量嵌入的智能代码搜索插件，支持多编程语言、多模型、GPU 加速、多引擎融合、**语言特定优化**。

## 特性

- 🔍 **语义搜索** - 使用自然语言查询代码
- 🚀 **混合引擎** - FastEmbed + CodeModel + Symbol 三层融合
- 🌐 **多语言支持** - 支持 16 种编程语言
- 🎯 **语言特定优化** - 针对不同语言的解析策略、分块大小、模型推荐
- 🤖 **多模型支持** - BGE、Jina、GTE、CodeT5 等最新嵌入模型
- ⚡ **GPU 加速** - 支持 CUDA 加速
- 🗄️ **LanceDB 存储** - 轻量级向量数据库
- 📊 **增量索引** - 支持增量更新索引
- 🎯 **轻量快速** - 使用 FastEmbed，无需 PyTorch（默认）

## 快速开始

### 安装依赖

```bash
cd plugins/semantic

# 安装所有依赖（包含 FastEmbed、CodeModel、解析器）
uv sync
```

### 初始化

```bash
/semantic init
```

**初始化会自动**：
- 检测项目使用的编程语言（只要存在文件就启用）
- 创建 `.lazygophers/ccplugin/semantic/` 数据目录
- 创建 `.lazygophers/.gitignore` 忽略插件数据
- 生成配置文件 `config.yaml`

### 建立索引

```bash
# 索引整个项目
/semantic index

# 增量索引（仅索引变更文件）
/semantic index --incremental
```

**索引会自动**：
- 创建余弦距离向量索引（≥100 个代码块时）
- 使用 FAISS IVF_PQ 索引加速搜索
- 显示 rich 进度条

### 搜索代码

```bash
# 自然语言搜索
/semantic search "如何读取文件"

# 搜索函数实现
/semantic search "实现二分查找"

# 中英文混合
/semantic search "用户认证 JWT token"
```

## 配置

### 查看配置

```bash
/semantic config
```

### 设置模型

```bash
# FastEmbed 模型
/semantic config --model bge-large-en

# CodeModel 模型（需要先启用引擎）
/semantic engines enable --engine codemodel
/semantic engines model --engine codemodel --model codet5+
```

### 相似度阈值

控制搜索结果的最小相似度要求，过滤掉低相关性的结果。

```bash
# 使用默认阈值（0.5）
/semantic search "用户登录"

# 自定义阈值
/semantic search "用户登录" --threshold 0.6
/semantic search "用户登录" -t 0.4
```

**阈值说明**:
- `0.4`: 宽松，适合初次探索
- `0.5`: **默认**，平衡准确度和召回率
- `0.6`: 严格，只返回高度相关的结果

配置文件设置：
```yaml
# .lazygophers/ccplugin/semantic/config.yaml
similarity_threshold: 0.5
```

**注意**：使用余弦距离向量索引，相似度范围 0-1，值越大表示越相关。

### 引擎管理

```bash
# 查看所有引擎
/semantic engines list

# 启用代码模型引擎
/semantic engines enable --engine codemodel

# 启用符号索引
/semantic engines enable --engine symbols

# 设置检索策略
/semantic engines strategy --strategy hybrid
```

**可用引擎：**
- `fastembed` - 快速向量搜索（默认）
- `codemodel` - 代码专用模型搜索（CodeT5/UniXcoder）
- `symbols` - 符号索引（函数/类名精确匹配）

**检索策略：**
- `fast` - 仅使用 FastEmbed
- `hybrid` - 混合搜索（融合所有引擎，推荐）
- `code` - 仅使用代码模型
- `symbols` - 仅使用符号索引

### 语言管理

```bash
# 列出支持的语言
/semantic languages list

# 启用语言
/semantic languages enable --lang rust

# 禁用语言
/semantic languages disable --lang java
```

## 架构

### 混合搜索引擎

```
┌─────────────────────────────────────────────────────────────┐
│                    Semantic Search Engine                    │
├─────────────────────────────────────────────────────────────┤
│  检索层                                                      │
│  ├── FastEmbed Engine (快速向量搜索)                         │
│  ├── CodeModel Engine (代码专用搜索)                         │
│  └── Symbol Index (符号精确匹配)                              │
├─────────────────────────────────────────────────────────────┤
│  融合层                                                      │
│  └── Result Fusion (多结果融合排序)                          │
├─────────────────────────────────────────────────────────────┤
│  存储层                                                      │
│  ├── LanceDB + FAISS (向量索引，余弦距离)                    │
│  └── 文件追踪 (增量索引)                                     │
└─────────────────────────────────────────────────────────────┘
```

### 向量索引

- **数据库**: LanceDB (Parquet 格式)
- **向量索引**: FAISS IVF_PQ (自动创建，≥100 代码块时)
- **距离度量**: 余弦距离 (cosine similarity)
- **相似度范围**: 0-1 (值越大越相关)

### 三层引擎对比

| 特性 | FastEmbed | CodeModel | Symbol |
|------|-----------|-----------|--------|
| 速度 | ⚡⚡⚡ | ⚡⚡ | ⚡⚡⚡ |
| 准确率 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 内存 | 低 | 高 | 极低 |
| 用途 | 快速初筛 | 代码语义 | 精确匹配 |

### 结果融合权重（混合检索）

- 符号匹配权重：30%
- FastEmbed 向量相似度：40%
- CodeModel 语义相似度：30%

## 支持的语言

- Python (.py)
- Go (.go)
- JavaScript (.js, .jsx, .mjs)
- TypeScript (.ts, .tsx)
- Rust (.rs)
- Flutter/Dart (.dart)
- Java (.java)
- Kotlin (.kt, .kts)
- C# (.cs)
- C (.c, .h)
- C++ (.cpp, .cc, .cxx, .hpp)
- Swift (.swift)
- PHP (.php)
- Ruby (.rb)
- Bash (.sh, .bash)
- Markdown (.md)

## 支持的模型

### FastEmbed 模型（快速向量搜索）

#### BGE 系列（推荐）
| 模型 | 维度 | 说明 |
|------|------|------|
| `bge-small-en` | 384 | 英文优化，轻量 |
| `bge-small-zh` | 512 | 中文优化 |
| `bge-base-en` | 768 | 英文通用 |
| `bge-large-en` | 1024 | 英文高精度 |

#### Jina 系列
| 模型 | 维度 | 说明 |
|------|------|------|
| `jina-small-en` | 512 | Jina 英文轻量 |
| `jina-base-en` | 768 | Jina 英文通用 |
| `jina-code` | 768 | Jina 代码专用 |

#### 其他
| 模型 | 维度 | 说明 |
|------|------|------|
| `all-minilm-l6-v2` | 384 | 极轻量，快速 |
| `multilingual-e5-small` | 384 | 多语言，轻量 |
| `gte-large` | 1024 | GTE 大型 |
| `nomic-embed-text-1.5` | 768 | Nomic 嵌入 |

### CodeModel 模型（代码专用搜索）

| 模型 | 说明 |
|------|------|
| `codet5+` | CodeT5+，代码理解最佳 |
| `unixcoder` | UniXcoder，多语言代码 |
| `graphcodebert` | GraphCodeBERT，图结构 |
| `codebert` | CodeBERT，代码预训练 |

完整模型列表运行: `/semantic models`

## 数据存储

所有数据存储在项目目录：

```
.lazygophers/ccplugin/semantic/
├── config.yaml          # 配置文件（YAML 格式）
└── lancedb/             # LanceDB 向量数据库
    └── code_index/      # 代码索引（余弦距离 + FAISS IVF_PQ）
```

**自动忽略**：
- `.lazygophers/.gitignore` 在初始化时自动创建
- Git 会自动忽略所有插件数据

## 性能优化

### GPU 加速

```bash
# 启用 GPU（默认）
/semantic config --gpu true

# 安装 GPU 版本（Linux/Windows）
uv pip install fastembed-gpu
```

### 增量索引

```bash
# 仅索引变更文件
/semantic-index --incremental
```

### 引擎选择

- **快速搜索**：仅启用 `fastembed`
- **高精度**：启用所有引擎，使用 `hybrid` 策略
- **精确匹配**：启用 `symbols`
- **代码语义**：启用 `codemodel`

### 模型选择

- **轻量快速**：`all-minilm-l6-v2` (384维)
- **中文项目**：`bge-small-zh` (512维)
- **高精度**：`bge-large-en` (1024维)
- **代码专用**：`codet5+` (CodeModel)

## 语言特定优化

Semantic 为不同编程语言提供针对性的优化策略，包括解析器、分块大小、模型推荐等。

### 高优先级语言（完整解析）

| 语言 | 分块大小 | 解析器 | 推荐模型 | 特性 |
|------|----------|--------|----------|------|
| **Python** | 500 | AST | codet5+ | 函数/类/装饰器/类型提示 |
| **Golang** | 400 | 正则 | jina-code | 函数/接口/结构体 |
| **Rust** | 300 | 正则 | unixcoder | 函数/结构体/特质/impl |
| **Flutter** | 450 | 正则 | codet5+ | Widget/类/方法 |
| **Java** | 450 | javalang AST | codet5+ | 类/接口/注解 |
| **Kotlin** | 420 | 正则 | codet5+ | 类/函数/对象/扩展 |
| **TypeScript** | 400 | 正则 | jina-code | 类/接口/类型 |

### 中优先级语言（基础解析）

| 语言 | 分块大小 | 解析器 | 推荐模型 |
|------|----------|--------|----------|
| JavaScript | 400 | 正则 | jina-code |
| C++ | 380 | 简单分块 | unixcoder |
| C# | 420 | 简单分块 | codet5+ |
| Swift | 400 | 简单分块 | codet5+ |

### 低优先级语言（简单分块）

| 语言 | 分块大小 | 推荐模型 |
|------|----------|----------|
| C | 350 | unixcoder |
| PHP | 380 | codet5+ |
| Ruby | 350 | codet5+ |
| Bash | 200 | multilingual-e5-small |
| Markdown | 1000 | multilingual-e5-large |

**注意**：
- Java 解析使用 `javalang` 库提供 AST 级精度（已默认安装）
- Rust 解析使用高质量正则（syn 是 Rust 原生库）
- 其他语言使用内置解析器

## 示例

### 搜索 API 用法

```bash
/semantic-search "如何使用 requests 库发送 HTTP 请求"
```

### 搜索算法实现

```bash
/semantic-search "实现快速排序算法"
```

### 搜索设计模式

```bash
/semantic-search "单例模式实现"
```

### 限定语言

```bash
/semantic-search "sort" --language python
```

### 查看统计

```bash
/semantic stats
```

## 最佳实践

1. **初始化后建立索引** - 首次使用前运行 `/semantic index`
2. **定期更新索引** - 代码变更后运行增量索引 `/semantic index --incremental`
3. **使用自然语言** - 描述功能而非语法（如"用户认证"而非"def login"）
4. **中英文混合** - 混合查询效果最佳
5. **选择合适阈值** - 默认 0.5，根据需要调整
6. **验证结果** - 检查代码是否匹配需求

## 技术栈

- **LanceDB** - 向量数据库 (Parquet 格式)
- **FAISS** - 向量索引 (IVF_PQ 算法)
- **FastEmbed** - 轻量级嵌入库 (ONNX Runtime)
- **sentence-transformers** - 代码模型 (CodeT5/UniXcoder)
- **Typer** - CLI 框架
- **Rich** - 终端美化 (进度条)

## 开发

```bash
# 进入插件目录
cd plugins/semantic

# 安装依赖
uv sync

# 运行测试
uv run pytest

# 代码格式化
uv run black scripts/
uv run ruff check scripts/
```

## License

AGPL-3.0-or-later

## 作者

lazygophers <admin@lazygophers.dev>

## 链接

- [GitHub](https://github.com/lazygophers/ccplugin)
- [文档](https://github.com/lazygophers/ccplugin/tree/master/plugins/semantic)
- [FastEmbed](https://github.com/qdrant/fastembed)
- [HYBRID.md](./HYBRID.md) - 混合架构详细文档

