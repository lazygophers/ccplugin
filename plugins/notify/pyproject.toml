[project]
name = "notify"
version = "0.0.61"
description = "Claude Code system notification plugin with hook support"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [ "pyyaml>=6.0.3", "typer>=0.21.1",]
